# [Project Name] — Codex Implementation Plan

## Rules for Codex

1. Follow `_docs/DESIGN.md` exactly — it is the source of truth
2. After each task: append to `_docs/IMPLEMENTATION_LOG.md`
3. After each phase: update `_docs/EXECUTIVE_SUMMARY.md`
4. Verify after every task: `go build ./...` + `go test ./...` + `npx tsc --noEmit`
5. Never skip the verify step
6. Commit after each task with conventional commit format

## Phase 1 — [Name]

### Task 1.1 — [Name]

**Files:** 
- Create: `path/to/file.go`

**Implementation:**
<!-- Steps -->

**Verify:** `go build ./...`

---
<!-- Add more tasks following this pattern -->
